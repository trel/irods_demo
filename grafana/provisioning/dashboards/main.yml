apiVersion: 1

providers:
  - name: 'My Dashboards'
    orgId: 1
    folder: '' # Optional: specify a folder name in Grafana
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards 
